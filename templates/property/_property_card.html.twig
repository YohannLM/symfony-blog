{#<div class="card mt-4">#}
{#    <div class="card-body">#}
{#        <h5 class="card-title">#}
{#            <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>#}
{#        </h5>#}
{#        <p class="card-text">{{ property.city }} ({{ property.postalCode }})</p>#}
{#        <div class="text-primary fs-2 fw-bold">{{ property.formattedPrice }}</div>#}
{#    </div>#}
{#</div>#}

<div class="card h-100">
    <img src="https://picsum.photos/seed/picsum/300/300" class="card-img-top" alt="Skyscrapers"/>
    <div class="card-body">
        <h5 class="card-title">
            <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
        </h5>
        <p class="card-text">
            {{ property.description }}
        </p>
        <div class="text-primary fs-2 fw-bold">{{ property.formattedPrice }}</div>
        <small class="text-muted">{{ property.surface }} m²</small>
    </div>
    <div class="card-footer">
        <small class="text-muted">{{ property.city }} ({{ property.postalCode }})</small>

    </div>
</div>
