<form method="post" action="{{ path('admin.property.delete', {'id': property.id}) }}" onsubmit="return confirm('Voulez vous supprimer ce bien ?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ property.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
